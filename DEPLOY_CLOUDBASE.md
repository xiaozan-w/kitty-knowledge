# 云端同步部署指南（CloudBase 腾讯云开发 · 免服务器）

「小琦的碎片库」用 **腾讯云 CloudBase（云开发）** 做云端同步层：**完全不需要你部署服务器、不跑 Docker、不装任何后端**。

只要在腾讯云开通 CloudBase、创建一个环境、拿到 **环境 ID** 填进设置，微信 / Chrome / 任意浏览器打开同一个网址，看到的都是同一份数据。

---

## 一、开通 CloudBase 并创建环境

1. 打开 https://console.cloud.tencent.com/tcb （用微信或 QQ 登录腾讯云）。
2. 首次进入会提示开通云开发，按引导 **新建一个环境**：
   - 环境名称：随意，例如 `kitty-kb`
   - 计费方式：选「按量计费」（新用户有免费额度，个人碎片库基本用不到付费）
3. 创建后进入环境，在 **环境总览** 页复制你的 **环境 ID**（形如 `kitty-kb-1gabc1234`）。

## 二、开启匿名登录（必须）

1. 左侧菜单进入 **环境 → 登录授权**。
2. 找到「匿名登录」，点击 **开启**。
   - 应用会自动用匿名身份连云端，无需你输入账号。

## 三、创建数据库集合并设置权限（必须）

1. 左侧菜单进入 **数据库 → 新建集合**，集合名填：`kitty_vault`
2. 建好后点进该集合，右上角「权限设置」选 **所有用户可读写**。
   - 因为碎片库要跨浏览器 / 设备共享，且匿名用户无固定身份，这里必须选「所有用户可读写」。环境 ID 是私密的，外人拿不到。
3. 无需手动建字段，第一次同步时应用会自动写入数据。

## 四、在工作台填环境 ID

1. 打开 https://xiaozan-w.github.io/kitty-knowledge/
2. 点左下角 **⚙️ AI 设置**
3. 在「☁️ 云端同步（CloudBase）」里粘贴 **环境 ID**
4. 点 **保存设置**（会自动连接并拉取云端数据），或点 **立即同步** 做一次双向同步。
5. 在微信、Chrome 等不同浏览器都填 **同一个环境 ID**，数据就互通了。

## 五、关于附件 / 大文件

- 你上传的每个文件会单独上传到 CloudBase **云存储**（类似网盘），云端数据库记录里只留一个 fileID 引用。
- 限制从「单条数据大小」变成「云存储总配额」——免费额度够个人长期用。
- 单个附件 >100MB 时会被本次同步跳过并提示，但 **文字照常同步**，不会整库失败。普通含图 / 扫描 PDF 都在 100MB 内，可正常跨浏览器同步与下载。

## 六、故障排除

| 现象 | 原因 / 解决 |
| --- | --- |
| 提示「连接云端失败」 | 环境 ID 复制错了（前后有空格）；或未开启匿名登录；或集合权限不是「所有用户可读写」 |
| 数据没同步 | 确认两个浏览器填的是 **同一个环境 ID**；点「立即同步」手动触发 |
| 附件下载不到 | 云存储文件被删；重新上传一次即可 |

## 七、备份

CloudBase 控制台数据库可随时导出。也可在工作台左侧点 **「⤓ 完整备份」** 导出一份 JSON 存本地，双保险。

> CloudBase 是「别人已经开好的服务器」，你只管填环境 ID 用，最省心。无需自建 server.js 或跑容器。
