# 部署到腾讯云 CloudBase 云托管（微信 / Chrome 数据互通）

目标：把本项目跑在腾讯云的「云托管」里，手机上**微信、Chrome 等任意浏览器打开同一个网址，看到的都是同一份数据**，且你不用在自己手机/电脑上跑任何服务。

---

## 一、开通云开发环境（一次性）

1. 打开 [腾讯云控制台](https://console.cloud.tencent.com/) → 搜索 **「云开发 CloudBase」** → 进入。
2. 点击 **「新建环境」**：
   - 环境名称随意，例如 `kitty-env`
   - 计费方式选 **「按量计费」**（新用户有免费额度，个人用几乎不花钱）
   - 开通后记住你的 **环境 ID**（形如 `kitty-env-1a2b3c4d`）
3. 进入该环境。

## 二、新建云托管服务

1. 左侧菜单 **「云托管」** → **「新建服务」**：
   - 服务名称：`kitty-knowledge`
   - 服务备注随意
   - 点击创建
2. 进入 `kitty-knowledge` 服务 → 点击 **「新建版本」**。

## 三、部署代码（二选一）

### 方式 A：本地代码包上传（最简单，推荐）
1. 把本项目文件夹压缩成 **zip**（注意不要压成双层目录，直接选里面的文件压缩）。
   - 必须包含：`server.js`、`package.json`、`Dockerfile`、`index.html`、`app.js`、`styles.css`、`assets/`、`icons/`、`manifest.json`、`sw.js`
2. 在「新建版本」里选择 **「代码包」→ 上传这个 zip**。
3. 配置：
   - **监听端口**：`3000`
   - **构建命令**：`npm install`（可留空）
   - **启动命令**：`node server.js`
4. 点击「开始部署」，等 1～3 分钟构建完成。

### 方式 B：关联 GitHub 自动部署（一劳永逸）
1. 在「新建版本」选 **「GitHub」**，授权并选择你的仓库 `xiaozan-w/kitty-knowledge`。
2. 分支 `main`，监听端口 `3000`，启动命令 `node server.js`。
3. 以后你 `git push` 后云托管会自动重新部署。

## 四、关键一步：挂载持久化存储（否则数据会丢！）

云托管容器重启时本地文件会清空，**必须**把数据目录挂到持久化存储：

1. 在版本设置的 **「高级设置 / 文件存储」** 里：
   - 开启 **「文件存储」**
   - **挂载路径**：填 `/data`
   - 容量按默认即可（个人用几 GB 足够）
2. 保存并重新部署一次。

> 本服务的所有数据（vault.json 和上传的附件）都会写入容器的 `/data`，对应上面挂载的持久化存储，**重启、重新部署都不会丢**。

## 五、拿到访问地址

1. 部署完成后，在「云托管」服务页会显示一个 **默认域名**，形如：
   `https://kitty-knowledge-xxxx.ap-shanghai.run.tcloudbase.com`
2. 点开这个地址，应该能看到「小琦的碎片库」启动页。
3. （可选）在「云托管」→「服务设置」里绑定你自己的域名（需备案）。

## 六、手机上使用

- 用**手机微信**打开这个地址 → 首次自动初始化预设板块，添加的记录会存到云端。
- 再用**手机 Chrome**（或任何浏览器）打开**同一个地址** → 看到的是同一份数据 ✅
- 任意一端增 / 删 / 改，约 1 秒内自动同步到云端，其他端刷新即见。

> 提示：微信内置浏览器和手机 Chrome 是两个独立环境，本地库互不相通——但因为现在数据在云端，所以两端看到的是同一份，完美互通。

---

## 七、AI 智能解析（可选）

- 打开应用内 ⚙️ 设置，填入 **豆包 API Key** 和模型 ID，点保存。
- Key 会保存在服务端的 `/data/config.json`，由服务端代理调用，**不会暴露在前端**。
- 不填也能用，只是 AI 概括功能走本地简单提取。

## 八、数据备份与导出

- 数据全在云托管的 `/data/vault.json`。
- 简单备份：在「云托管」版本里可以通过「登录容器」或挂载的「文件存储」控制台下载 `vault.json`。
- 应用内也保留了「导出离线备份」按钮（生成单文件版，存到本机）。

## 九、常见问题

- **打开是旧页面？** 云托管默认不缓存，若看着旧，硬刷新（微信：右上角菜单→刷新；Chrome：地址栏旁刷新长按→硬性重新加载）。
- **数据没同步？** 确认访问的是同一个云托管地址；确认第四步的 `/data` 文件存储已挂载。
- **GitHub Pages 还能用吗？** 能，但那是纯本地版（数据只存在当前浏览器），和云托管是两个独立环境。建议手机只用云托管的地址。

---

## 本地调试（可选，电脑上）

```bash
cd kitty-knowledge
node server.js            # 默认 http://localhost:3000
# 或指定端口和数据目录
PORT=8080 DATA_DIR=./data node server.js
```
