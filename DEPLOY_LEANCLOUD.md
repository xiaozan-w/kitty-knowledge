# 云端同步部署指南（LeanCloud · 免服务器）

「小琦的碎片库」用 **LeanCloud** 做云端同步层：**完全不需要你部署服务器、不跑 Docker、不装任何后端**。
只要在 LeanCloud 注册一个免费账号、拿到两个 key 填进设置，微信 / Chrome / 任意浏览器打开同一个网址，看到的都是同一份数据。

---

## 一、注册并创建应用（一次性，约 3 分钟）

1. 打开 https://www.leancloud.app/ （国内版），点右上角「注册」。
   - 个人用**免费版（开发版）**即可，有充足额度。
   - 可能需要手机号验证。
2. 登录后进入控制台，点「创建应用」→ 起个名字（如 `kitty-vault`）→ 创建。
3. 进入该应用，左侧「设置」→ **「应用凭证」**，你会看到：
   - **App ID**（形如 `xxxxxxxxxxxxxxxxxxxxxxxx`）
   - **App Key**（形如 `xxxxxxxxxxxxxxxxxxxxxxxx`）
   - 把它们复制下来。

> 数据存在**你自己的这个 LeanCloud 应用**里，不经由任何第三方。免费版足够个人长期使用。

---

## 二、在工作台里填 key（每台设备都填一次）

1. 用手机/电脑浏览器打开你的网址：
   `https://xiaozan-w.github.io/kitty-knowledge/`
2. 点左下角 **⚙️ 同步与 AI 设置**。
3. 在「☁️ 云端同步」下填：
   - **LeanCloud App ID** → 粘贴刚才的 App ID
   - **LeanCloud App Key** → 粘贴刚才的 App Key
   - **API 域名** → 留空（默认用国内域名，除非你用的是国际版）
4. 点 **「保存设置」** → 会自动连云端并拉取（若云端为空则先传本地数据）。
5. 也可以点 **「立即同步」** 先推一次本地数据到云端。

第一次同步时，LeanCloud 会自动创建一张叫 `KittyVault` 的表，数据就存在那里。

---

## 三、多设备 / 多浏览器互通

- **微信里**打开网址、填同一组 App ID/Key → 数据写进云端
- **Chrome 里**打开同一网址、填同一组 App ID/Key → 拉到的就是同一份
- 任意一端新增/修改/删除，**约 1 秒内自动同步**到云端，另一端刷新即见

> 所有设备必须填**完全相同的 App ID 和 App Key**，才会共享同一份数据。

---

## 四、数据说明（容量够不够用？）

> 结论：**你每日常规上传图片/PDF 完全够用，不用担心再出现之前 Gist 那种"撑爆"失败。**

- **附件不再塞进单条记录。** 每条记录（板块/模块/文字）很小，整体作为一条 `KittyVault` 数据存（文本，通常几十 KB～几 MB）。
- **你上传的每个文件**会单独上传成 LeanCloud 的「文件存储」对象（对象存储，类似网盘），云端记录里只留一个**下载链接**引用它。
- 所以限制从「单条 16 MB」变成了「**你这个 LeanCloud 应用的文件存储总配额**」——免费开发版有 **GB 级**额度，个人碎片库长期用都用不完。
- 单个附件文件：LeanCloud 对大文件会自动分片上传，正常文档、图片、PDF（几十 MB 内）都没问题；只有极端超大文件（>15 MB）会被本次同步跳过并提示，但**文字照常同步**，不会整库失败。
- 之前 Gist 的 1 MB、以及旧版把整库 base64 塞进单对象撞 16 MB 的坑，现在架构上已经绕开了。

---

## 五、常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 填了 key 仍不同步 | 确认微信和 Chrome 填的是**同一组** App ID/Key；点「立即同步」看提示 |
| 提示「连接云端失败」 | App ID/Key 复制错了（前后有空格）；或 LeanCloud 免费版需先「激活」应用 |
| 数据没出现 | 云端已有数据时会**以云端为准**覆盖本地；先确认哪端是「最新」的那份 |
| 改了代码要重新发布 | 本仓库推到 GitHub 后，GitHub Pages 自动重建；手机强制刷新页面即可 |

---

## 六、数据备份（可选但建议）

LeanCloud 控制台里可随时「导出」数据。也可以在工作台左侧点 **「⤓ 完整备份」** 导出一份 JSON 存到本地，双保险。

---

> 之前试过的 Cloudflare Worker / 自建 server.js 方案已弃用：它们都需要你长期开着一台服务器，手机上不方便。LeanCloud 是「别人已经开好的服务器」，你只管填 key 用，最省心。
